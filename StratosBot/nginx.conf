server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index stratosbot.html;

    location / {
        try_files $uri $uri/ /stratosbot.html =404;
    }

    location ~* \.(html|css|js|ico|svg|woff2?)$ {
        add_header Cache-Control "public, max-age=3600";
    }
}
